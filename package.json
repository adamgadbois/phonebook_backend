{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../fullstackopen/part3/phonebook_frontend/ && npm run build && cp -r build ../../../phonebook_backend",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adamgadbois/phonebook_backend.git"
  },
  "author": "Adam Gadbois",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/adamgadbois/phonebook_backend/issues"
  },
  "homepage": "https://github.com/adamgadbois/phonebook_backend#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
